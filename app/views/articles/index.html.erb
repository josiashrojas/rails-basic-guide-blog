<h1>Articles</h1>

<p class="text--black-300">Our blog has <span class="text--primary text--bold"><%= Article.public_count %> articles</span> and counting!</p>

<ul class="article__list">
    <% @articles.each do |article| %>
        <% unless article.archive? %>
            <li>
                <%= link_to article.title, article %>
            </li>
        <% end %>
    <% end %>
</ul>
<%= link_to "New Article", new_article_path, class: "btn btn--border" %>